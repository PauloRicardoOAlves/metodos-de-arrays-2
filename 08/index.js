const frutas = ["Manga", "UVA", "abacaxi", "banaNA", "MAçã"]
let resultado = []
let contagem = 1

function primeiraLetraMaiuscula(array) {
    for (let i = 0; i < array.length; i++) {
        let palavra = primeiraLetraMaiuscula[i].toLowerCase()
    }
}